# 复制为 .env.local 后填入，仅本地开发用；线上走 Cloudflare Pages 环境变量
DASHSCOPE_API_KEY=

# --- 飞书推送（可选，不配则「推送到飞书」按钮明确报错，不影响其余功能）---
# 群设置 → 群机器人 → 添加机器人 → 自定义机器人，创建后拿到的地址
FEISHU_WEBHOOK_URL=
# 只有机器人的「安全设置」勾了「签名校验」时才需要；没勾就留空
FEISHU_WEBHOOK_SECRET=
